#include<stdio.h>

void strcpy(char *str1, char *str2) {
	for (int i = 0; str2[i] != '\0'; i++) {
		str2[i] = str1[i];
	}
	
}
int main()
{
	char str1[100];
	char str2[100];
	if(scanf_s("%s", str1, sizeof(str1))==1);
	printf("str : %s\n", str1);
	strcpy(str1,str2);
	printf("strcpy : %s\n", str2);

	return 0;
}